#!/bin/bash

# Documentation is served by the docs container at http://localhost:8080
# Start it with the rest of the services:
#
#   docker-compose up -d
#
# To start only the docs container:
#
#   docker-compose up -d docs

echo "Documentation is available at http://localhost:8080"
echo "Make sure the docs container is running: docker-compose up -d docs"
